This is the folder where the top level CMake file for the Message Framework will go.
The file is called "CMakeLists.txt" and will be the source file that kicks off the
compilation of the project and any subprojects (which will also need CMakeList.txt
files of their own).